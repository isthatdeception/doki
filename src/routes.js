/**
 * routes that will be used in all over the app
 * expected routes
 */

export const FEED = "/";
export const LOGIN = "/login";
export const SIGN_UP = "/signup";
export const PROFILE = "/p/:username";
export const NOT_FOUND = "/not-found";
export const POST = "/d/:post";
